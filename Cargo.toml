[package]
name = "the_world"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.18", features = ["bevy_dev_tools"] }

[profile.dev]
opt-level = 0
debug = true

[profile.release]
opt-level = 3
lto = true
